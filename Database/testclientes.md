```sql
    CREATE TABLE clients (
        id INT AUTO_INCREMENT PRIMARY KEY,
        first_name VARCHAR(255),
        last_name VARCHAR(255),
        document VARCHAR(20),
        phone VARCHAR(20),
        email VARCHAR(255),
        state TINYINT
    );

```
